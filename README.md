# Node + Gcloud + Firebase Builder Image

This repository builds a docker image used for building source from a node application that uses google cloud and firebase tools. This is just the image that is used to make the builds, not the source code of any specific app.

Builds via a jenkinsfile.

